# Copy to .env.prod and fill in the values
# Never commit .env.prod to version control

# Phoenix
SECRET_KEY_BASE=        # mix phx.gen.secret
PHX_HOST=seu-dominio.com
PORT=4000
PHX_SERVER=true

# Database — must match POSTGRES_* below
DATABASE_URL=ecto://haru:SENHA_FORTE@postgres/haru_prod
POOL_SIZE=10
DATABASE_SSL=false

# PostgreSQL (used by the postgres service)
POSTGRES_USER=haru
POSTGRES_PASSWORD=SENHA_FORTE
POSTGRES_DB=haru_prod
